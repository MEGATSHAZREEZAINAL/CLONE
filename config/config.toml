# Global LLM configuration - Using Google Gemini
[llm]
model = "gemini-1.5-pro"
base_url = "https://generativelanguage.googleapis.com/v1beta"
# Google Gemini API Key configured
api_key = "AIzaSyAITnrTX617C7X1N4RnCZ9LfM5j7Rx7KP4"
max_tokens = 4096
temperature = 0.0

# Example configurations for different providers:

# [llm] # OpenAI GPT-4o
# model = "gpt-4o"
# base_url = "https://api.openai.com/v1"
# api_key = "sk-your-openai-key-here"
# max_tokens = 4096
# temperature = 0.0

# [llm] # Anthropic Claude
# model = "claude-3-5-sonnet-20241022"
# base_url = "https://api.anthropic.com"
# api_key = "sk-ant-your-anthropic-key-here"
# max_tokens = 4096
# temperature = 0.0

# [llm] #AZURE OPENAI:
# api_type= 'azure'
# model = "YOUR_MODEL_NAME" #"gpt-4o-mini"
# base_url = "{YOUR_AZURE_ENDPOINT.rstrip('/')}/openai/deployments/{AZURE_DEPOLYMENT_ID}"
# api_key = "AZURE API KEY"
# max_tokens = 8096
# temperature = 0.0
# api_version="AZURE API VERSION" #"2024-08-01-preview"

# Optional configuration for specific LLM models
[llm.vision]
model = "gemini-1.5-pro"
base_url = "https://generativelanguage.googleapis.com/v1beta"
# Using the same Gemini API key for vision tasks
api_key = "AIzaSyAITnrTX617C7X1N4RnCZ9LfM5j7Rx7KP4"

# Optional configuration for specific browser configuration
[browser]
# Whether to run browser in headless mode (recommended for production)
headless = true
# Disable browser security features (required for automation)
disable_security = true
# Extra arguments to pass to the browser
extra_chromium_args = ["--no-sandbox", "--disable-dev-shm-usage"]
# Path to a Chrome instance to use to connect to your normal browser
# e.g. '/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'
#chrome_instance_path = ""
# Connect to a browser instance via WebSocket
#wss_url = ""
# Connect to a browser instance via CDP
#cdp_url = ""

# Optional configuration, Proxy settings for the browser
# [browser.proxy]
# server = "http://proxy-server:port"
# username = "proxy-username"
# password = "proxy-password"
